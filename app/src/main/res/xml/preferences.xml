<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <CheckBoxPreference
        android:key="pref_record_location"
        android:title="Record Location"
        android:summary="Add location information to measurements as they are recorded" />
    <ListPreference
        android:key="pref_significant_digits"
        android:title="Significant Digits"
        android:summary="Sets the number of significant digits to display for a value"
        android:defaultValue="3"
        android:entries="@array/listArray"
        android:entryValues="@array/listValues"/>
</PreferenceScreen>